/* LEEWAY HEADER — DO NOT REMOVE (config shell; no secrets) */
window.AGENTLEE_CONFIG = {
  // Leave empty by default; Worker URL is produced by CI deploy and can be injected if desired.
  GEMINI_PROXY_URL: ""
};
