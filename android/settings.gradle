/*
 LEEWAY HEADER
 TAG: ANDROID.GRADLE.SETTINGS
 COLOR_ONION_HEX: NEON=#39FF14 FLUO=#0DFF94 PASTEL=#C7FFD8
 ICON_FAMILY: lucide
 ICON_GLYPH: settings
 ICON_SIG: CD534113
 5WH: WHAT=Gradle settings for Android app module; WHY=Configure Capacitor + module includes; WHO=Leeway Core; WHERE=android/settings.gradle; WHEN=2025-10-19; HOW=Gradle
 SPDX-License-Identifier: MIT
*/

include ':app'

// NOTE: The Cordova aggregator module is optional and generated by "npx cap sync" when Cordova plugins are present.
// It has been removed because android/capacitor-cordova-android-plugins/ is missing in this repo.
// If you add Cordova plugins later, run: npx cap sync android

// Only apply Capacitor settings if the generated file exists
if (file('capacitor.settings.gradle').exists()) {
	apply from: 'capacitor.settings.gradle'
} else {
	logger.lifecycle("[Gradle] Skipping capacitor.settings.gradle (not found). Run `npx cap sync android` after adding plugins.")
}